<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : t('app.name') %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', skip_pipeline: true, :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', skip_pipeline: true, :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', skip_pipeline: true, :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', skip_pipeline: true, :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', skip_pipeline: true, :rel => 'shortcut icon' %>

  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
                    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
                    <![endif]-->
  </head>
  <body data-lang="<%= I18n.locale %>">

    <div class="navbar navbar-default navbar-static-top">
      <div
        class="container">
        <%# Для вывода названия приложения используем хелпер I18n.t %>
        <%= link_to t('app.name'), root_path, class: 'navbar-brand' %>

        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <li><%= link_to t('buttons.profile'), current_user %></li>
              <li><%= link_to t('buttons.log_out'), destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
            <% else %>
              <li class="nav-item"><%= link_to t('buttons.sign_in'), new_user_session_path, class: 'nav-link active fw-light' %></li>
              <li class="nav-item"><%= link_to t('buttons.sign_up'), new_user_registration_path, class: 'btn btn-outline-primary rounded-pill fw-light' %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= flash_messages %>
          <%= yield %>
        </div>
        <div class="col-lg-3"></div>
      </div>


      <footer class="container mt-auto d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top mb-0">
        <div class="col-md-4 d-flex align-items-center">
          <a href="https://goodprogrammer.ru/">
            <span class="text-muted">&copy;
              <%= t('app.company') %>
              2022</span>
          </a>
        </div>

        <ul class="nav col-md-4 justify-content-end list-unstyled d-flex">
          <li class="ms-3">
            <span class="text-muted"><%= t('app.creator') %></span>
          </li>
          <li class="ms-3">
            <a class="text-muted" href="https://github.com/ProfessorNemo"><img src="<%= asset_path('gh.png') %>" class="bi" width="30" height="30"></a>
          </li>
        </ul>
      </footer>
    </body>
  </body>
</html>
